# Initialization

To initialize a project, bender provides a convenience command to set up an initial empty manifest.

```sh
bender init
```

This creates an initial `Bender.yml` manifest file containing the required package information and a harness for adding dependencies and sources.
