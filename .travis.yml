language: rust
rust:
  #- stable
  - "1.35.0"
  #- beta
  - nightly
  # minimum supported version
  - "1.31.0"
matrix:
  allow_failures:
    - rust: nightly

script:
  - cargo build
  - cargo test --all
  - tests/run_all.sh
