package:
  name: pickle_repo

sources:
  - include_dirs:
      - include
    defines:
      ENABLE_LOGGING: 1
    files:
      - src/common_pkg.sv
      - src/bus_intf.sv

  - target: top
    files:
      - src/top.sv
